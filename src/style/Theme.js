const size = {
  mobile: '767px',
  desktop: '768px',
};

const theme = {
  mainColor: 'pink',
  subColor: 'gray',
  mobile: `(max-width: ${size.mobile})`,
  desktop: `(min-width: ${size.desktop})`,
};

export default theme;
